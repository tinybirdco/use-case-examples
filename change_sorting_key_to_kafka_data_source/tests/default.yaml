- recent-visits-exist:
    max_bytes_read: null
    max_time: null
    sql: |
        SELECT count() AS cnt
        FROM kafka_products
        WHERE __timestamp >= NOW() - INTERVAL '1 month '
        HAVING count() = 0
