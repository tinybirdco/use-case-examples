NODE start_syncing
SQL >
    %
    SELECT * EXCEPT (version), toUInt32(version) as version
    FROM v1_0_1.analytics_events
    WHERE timestamp BETWEEN {{ DateTime(start_date) }} AND {{ DateTime(end_date) }}

TYPE COPY
TARGET_DATASOURCE analytics_events
COPY_SCHEDULE @on-demand
