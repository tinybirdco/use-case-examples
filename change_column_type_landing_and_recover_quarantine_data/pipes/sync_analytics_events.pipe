NODE start_syncing
SQL >
    SELECT * EXCEPT (session_id), toUInt32(session_id) as session_id
    FROM v1_0_1.analytics_events
    WHERE timestamp > '2024-02-28 13:10:00'

TYPE MATERIALIZED
DATASOURCE analytics_events
